# ============================================================
#  签名配置模板
#
#  用法：复制这个文件为同目录下的 keystore.properties，然后填入真实值。
#      cp keystore.properties.example keystore.properties
#
#  ⚠️ keystore.properties 已经被 .gitignore 忽略，不要提交进仓库。
#  ⚠️ 这个文件里的密码丢了就找不回来，丢了之后已发布的 App 无法再更新。
#     请把 keystore 文件和密码一起备份到 U 盘 / 密码管理器，至少两份。
# ============================================================

# 密钥库文件的位置。两种写法都支持：
#   相对路径：release.jks        → 解析为 android/release.jks
#   绝对路径：G:/keys/release.jks
storeFile=release.jks

# 密钥库密码（keytool 里 -storepass 那个）
storePassword=改成你的密钥库密码

# 密钥别名（keytool 里 -alias 那个）
keyAlias=fridgeprophet

# 密钥密码（keytool 里 -keypass 那个）
# 如果生成时 -keypass 和 -storepass 填的一样，这里也写一样的
keyPassword=改成你的密钥密码
